import { Terminal } from "./terminal";

import {default as TerminalModule} from "./terminalModule"

import { add, rng } from "./utils/math";
import { delay } from "./utils/timing";

export { Terminal, TerminalModule, add, rng, delay };
